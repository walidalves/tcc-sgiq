@model IEnumerable<Sgiq.Dados.Models.Metrica>

@{
    ViewData["Title"] = "Métrica";
}



<div class="col-lg-12">
    <div class="row">
        <div class="col-md-12">
            <div class="pull-right">
                <a asp-action="Create" class="btn btn-primary"><i class="fa fa-plus"></i> <span>Novo</span></a>
            </div>
        </div>
    </div>


    <div class="card mt-3">
        <div class="card-header">
            Métricas
        </div>
        <div class="card-body">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            Nome
                        </th>
                        <th>
                            Descrição
                        </th>
                        <th>
                            Frequência de Aferição
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Any())
            {

                        @foreach (var item in Model)
                {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Nome)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Descricao)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.FrequenciaAfericao.Nome)
                                </td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@item.MetricaId">Edit</a> |
                                    <a asp-action="Details" asp-route-id="@item.MetricaId">Details</a> |
                                    <a asp-action="Delete" asp-route-id="@item.MetricaId">Delete</a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5">Nenhum Registro Encontrado</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>